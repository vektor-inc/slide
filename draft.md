# 全自動並列AI駆動開発システムのしくみ

AIを使ってコードを書くだけではなく、

複数のAIに役割を分担させる
並列でタスクを進める
レビューや修正まで自動化する
人間は全体の判断に集中する
といった開発フローを、効率よく実現するために行った

エージェントの作成
統合UIの作成
自動化システムの作成
モバイルからの制御
について紹介します。

---


* 動作デモ
* ルールを作る
* スキルを作る
* エージェントを作る
* 統合UIを作る

## 

※ はじめに

本セッションでは、AIコーディング自体に慣れてない人にも感覚がわかるように、
ロードマップ的に説明します。
得に前半の説明では「いやいや、そんなやり方しねーよ」って手法から入りますが、
なぜ現状の手法にたどり着くのかを説明するためにあえてセオリーでないかもしれないやり方から説明します。
あらかじめご了承ください。

---

## 仕様は先に作ろう

本セッションの範囲ではないので詳しくは触れませんが、

実際にAIに実装させる前には、AIとディスカッションなどして、作るものの仕様は固めましょう。

* 何を目的とするものなのか
* 技術的な仕様

など、仕様はプロジェクトフォルダの ./docs/ ディレクトリなどに纏めて用意した上で実装してもらいましょう。

---

* どう仕様を作ればいいのかわからない
* 仕様の精度を上げたい

できあいのスキルを使いましょう。

例) superpowers

https://github.com/obra/superpowers

※ 意味がよくわからない人はとりあえず日本語に翻訳してインストールしてみて

---

### 仕様書は AGENTS.md から明示する

./docs/ などのディレクトリに仕様書を置いても明示しないとAIは参照しないので、読み込むように指定します。

```
./docs/ 以下に仕様を纏めてあるので、各作業でAIが参照するようにプロジェクト直下の AGENTAS.md を設定してください。
```

みたいに投げれば良きにはからってくれます。

---

## ルールの整備

---

### 事前に思いつくコーディング規約などを整備

実装にあたって持たせておきたいルールやスキルを設定

* 一般的にスタンダードな規約は読み込ませる
  - WordPressなら WordPress Coding Standards。
  - Pythonなら PEP 8 (命名、整形、docstring)。
  - Goなら gofmt と Go のレビュー規約。

よくわからなければ

「◯◯◯ の開発をするにあたって、
  コーディング規約を作っておきたいです。
　どんなルールにしておくのが一般的？」

みたいに雑に聞いてもそれなりに案内してくれます。

---

### その他事前に用意するルール例

* 関数やメソッドの命名規則
* CSSの命名規則
* デザインシステム
* チーム内の共通独自ルールなど

---

### ルールの保存先

#### 全プロジェクト共通

* Users/ユーザー名/.claude/rules/
* Users/ユーザー名/.codex/rules/

など、ユーザー直下に「. + エージェント名」のディレクトリに .md ファイルで保存されます。

---

### ルールの種類ごとにわける

* Users/ユーザー名/.claude/rules/coding-rules.md
* Users/ユーザー名/.claude/rules/design-rules.md
* Users/ユーザー名/.claude/rules/css-rules.md
* Users/ユーザー名/.claude/rules/e2e-test-rules.md

必要な時だけルールを読み込まないと
* 無駄にトークンを消費する
* 不要なルールまで読み込むと精度が落ちる

---
#### PHPUnit テストの形式
* 自動でテストコードを量産されてもそのテストコードが何をしているのかもわかりにくい
  - 設定値と期待値をセットで記載して、そこだけ確認すれば抑えないといけない動作の漏れがないか確認できるように


---

#### プロジェクト固有のルール

プロジェクトディレクトリ直下の「. + エージェント名」のディレクトリに保存されます。

このあたりは深く考えなくても最初は AI がよしなにしてくれます。

---

## 初期実装

---

### 仕様やルールができたら部分的にテスト実装

最初は Claud Code や Codex みたいなコマンドで実装するのではなく、
VS Code や Cursor などで部分的に実装してもらいます。

AIが実装した内容が確認・チューニングするため

---

### 実際に実装してもらうと...

「いや...そうじゃないんだなぁ...。」

という不満が沢山出ると思います。

---

### ルール化してあったのに...

ルールはある程度読ませてあっても、実際にプロが今まで気を配っていた細かい知識は膨大

* 関数やCSSの命名（一般的ではなく独自ルール）
* テストの書き方
* UIでの説明文のわかりやすさの粒度
* マークアップの作法
* 余白のとり方
* デザインの視線誘導

---

→ A. やっぱりAIは使えない
→ B. 何がダメだったのか、言語化してルールをブラッシュアップしていく

---

### ルールは定期的にブラッシュアップする

運用しているとどんどん追加されて長くなる。

#### 長ければいいわけではない

* AIのトークン消費量が増える
* 長いとそれだけノイズが増えて、ルールが守られなく鳴る。

```
既存のルールファイルについて、表現が誤解を招きやすかったり、説明が冗長になっているか確認して、
AIが誤解無くルールを理解できるようにブラッシュアップしてください。
```

というように精度の見直しをさせます

---

### コーディング以外でもルール化

初期実装させて、以降引き続きプログラムを修正していくと...

ご存知の通りコミットやプルリクエストの作成を繰り返す事になります。

* コミットメッセージを考えるのが面倒
* プルリクの本文を書くのが面倒

→ AI に「コミットして」「プルリク出して」って言えばやってくれるけど...

---

当然今まで社内でやってたルールと違うフォーマットだったりする

* コミットメッセージのルール
* プルリク本文に書く内容や情報の粒度

→ ルール化

---

ルールがあっても人力だと...

* 社内でルールがあっても守らない人がいる
* PRの本文など、雑に書かれると何をチェックすればいいのか意味がわからずレビューに時間がかかる

---

## スキルの作成

---

ルールがある上で、

* 普通に「プルリク出して」とAIに伝えても ルールファイルを読み込んでくれなかったりする
* 「.claude/rules/pull-request-rules/ を読んでプルリク出して」と毎回言うのもだるい

→ スキル化します。

---

### スキルとは？

* よく行う作業の手順書のようなもの
* コマンドで簡単に呼ぼ出せる

---

### スキル例

```
---
name: vk-pr
description: "PR ルールに従い、コミット・changelog 記載・確認手順を含む PR を作成する。PR 作成を依頼されたときに使用"
---

# /vk-pr スキル

このスキルは `rules/pull-request.md` の PR ルールに従い、**PR 作成まで** を担う。

〜 以下略　〜
```

---

（実際の pull-request.md で軽く解説）

---

./claude/skills/pull-request.md
./claude/skills/edit-title.md

など、ユーザー直下に `.エージェント名/skills/` のディレクトリに .md ファイルで保存されます。

---

### 人間がボトルネックになる

開発・PR作成をAIがするようになる
→ レビューの数が増える
→ 手動確認とかめんどくさくてやってられない

→ レビュー用のスキルを作る

※ 標準で code-review ってスキルもあるけどね...

---

### レビュー用のスキルも作る

ルールがあっても実装者が100%それに従うとは限らない。

* タイトルがルール通りになってるか
* PR本文がルール通りになってるか？
* 各種コーディングルールが守られているか？
* PHPUnit や e2eテスト がルール通りに書かれているか？
* セキュリティチェック
* UXチェック
* e2eテストを実施＆スクリーンショット撮影
* 結果報告

→ スキルとして登録しておけば同じ処理を自動でしてくれる

---

### ルールとスキルの棲み分けが効いてくる

* 実装やPRを立てる時
* レビューする時

ルールは同じ。

実装スキルとレビュースキルで同じ注意事項をそれぞれ書くと...
二重、三重管理になっていく。

* ルールのばらつき
* 片方の変更・追加漏れ

実装スキルもレビュースキルも、同じルールファイルを参照させる。

---

## 自動処理スキルの作成

「PRスキル」「レビュースキル」ができるなら...

* issue に対する実装前の仕様検討・提案
* 実装後にレビューでひっかかった場合の自動修正

→ 組み合わせたスキルを作れば issue を  
  計画 → 実装 → テスト → レビュー（必要に応じて差し戻し修正）  
  まで一気にやってくれる自動化スキル作れるんじゃない？

---

## サブエージェントの作成

---

### 1人のAIに全部やらせると...

* 実装
* テスト
* UI / UX レビュー
* セキュリティレビュー
* PR 作成
* 進行管理

全部同じAIが、自分で実装して、自分でレビューすることになる。

人間のチームに置き換えると、ちょっと不自然。

---

### 役割を持ったエージェントを作る

* ディレクター
* WordPress実装担当
* UI / UX担当
* ブラウザテスト担当
* コード品質・セキュリティ担当

役割ごとに、参照するルール・使うスキル・完了条件を変える。

---

### エージェントは「名前をつけたAI」ではない

必要なのはキャラクター設定よりも、責任範囲の明文化。

* 何を担当するのか
* 何を判断してよいのか
* どのルールを読むのか
* どこまで作業したら完了なのか
* 誰に、何を報告するのか

...。

---

ってこの部分の原稿はAIさんが提案したのですが...

---

わかってない  
┐(´д｀)┌

---

エージェント設定に必要なのは...

----

<div style="font-size:3rem;margin-bottom:3rem;">
＿人人人人人人人人人人人人人＿<br>
＞　キャラクター設定だ！　＜<br>
￣Y^Y^Y^Y^Y^Y^Y^Y^Y^Y^Y￣
</div>

---



---

### 役割を分けるメリット

#### コンテキストを分離できる

実装担当は実装に必要な情報へ集中。
レビュー担当は完成した差分を第三者視点で確認。

#### 並列で進められる

依存しない調査・テスト・レビューは同時に実行できる。

#### チェックの独立性が上がる

「自分が書いたコードだから大丈夫」という前提を持たずに確認できる。

---

### 作成したエージェント

| 担当 | 主な役割 |
|---|---|
| 司 | 全体の進行管理・担当者への割り振り |
| 和田 | 主に WordPress 関連などプログラムの実装 |
| 植草 | UI設計・UX・アクセシビリティ |
| 麗美 | Playwright によるブラウザ動作テスト |
| 安藤 | コード品質・セキュリティの最終レビュー |

---

エージェントの設定ができたので
issue を自動処理するスキルを作ります。

---

やりたい事

* issue のURLを投げる
* 司さん（ディレクター）が中身を確認
  - 開発担当 / UX担当 / セキュリティー担当など、  
	必要なサブエージェントを起動して仕様を議論させる
    - 議論の結果人間に方針確認が必要なもの  
	  → issue に仕様提案をコメントして指示を待機
    - 議論の結果人間の確認が不要と判断したもの  
	  （明確は不具合修正や issue で既に仕様がしっかり固まっているもの）  
	  → 実装開始
* 実装完了したら  
  - PHPUnitテスト実行
  - 各種レビュー
    - 安藤さんのセキュリティレビュー
    - 植草君の UX レビュー
    - 麗美ちゃんの e2e テスト
  - 問題があれば差し戻して和田君が修正 → 再レビュー
* 修正完了したら vk-pr スキルでプルリク作成
* プルリクの内容がプルリクのルールに沿っているか司さんが確認
  - 問題があれば修正
* 司さんがPRのコメントで人間に最終報告

---

どうやって作るのか？

---

「こういう流れで処理したいんだけどスキル作って！」

---

### issue 自動処理スキル完成！

URL投げれば issue が自動で完了する

/vk-kore {url} スキル（これやっといてスキル）爆誕！

---

### チューニングは必要

スキルができると「うひょー！これで超ラクだぜ！ヒャッハー！」と思いがちですが、
意図せず途中で止まったりするので、都度ブラッシュアップしていきます。

---

## 並列実行

---

### タスクが1件ずつなら、まだ普通のAIコーディング

AIは待ち時間が長い。

* 実装中
* テスト実行中
* 外部レビュー待ち
* CI待ち
* 修正後の再確認中

1件が終わるまで待ってから次へ進むのはもったいない。

---

### 複数のタスクを同時に動かす

VS Code や Cursol などのエディタで実行するのではなく、
ターミナルを複数開いて同時に処理させる

```
タスク A ─ 実装 ─ テスト ─ レビュー ─ PR
タスク B ─ 実装 ─ テスト ─ レビュー ─ PR
タスク C ─ 実装 ─ テスト ─ レビュー ─ PR
```
---

### 標準ターミナルをたくさん開いてみた

並列処理自体はできる。

でも数が増えると...

* どのターミナルが、どのタスクかわからない
* どれが処理中で、どれが入力待ちかわからない
* ウィンドウを順番に開いて確認する必要がある
* 終了したセッションを見落とす
* ターミナルを並べ直すだけでも面倒

---

### AIの処理速度より、人間の監視がボトルネックに

並列数を増やすほど、
人間がターミナルを見回る仕事が増えていく。

→ 複数セッションの状態を一覧できるUIが必要。

---

## 統合UIの作成

---

### VK Terminals

複数のターミナルを自動折返しグリッドで表示する
Electron 製デスクトップアプリを作成。

https://github.com/vektor-inc/vk-terminals/

---

### 複数のClaudeを一画面で操作

* ペインごとに Claude Code を起動
* ペインを追加・並べ替え・リサイズ
* 不要なペインはサイドバーへ格納
* Claude の使用量を表示

---

### 統合UIで「見やすく」はなった

* 個別にターミナルのウィンドウがばらつくよりはかなり見やすくなった
* 入力待ちの場合にラベルがつくようにした

けれど...

* どのペイン（ターミナル）が、どのタスクかわかりにくい
  issue やプルリクの URL が探しにくい。ブラウザのタブが散らかる

---

加えて、まだ人間がやっ加えて事が多い

* issue を確認する
* Claude にタスクを投入する
* PR・CI・レビューの状態を確認する
* 完了・失敗を判断する
* 次のタスクを投入する

---

### 人間がディスパッチャーになっている

AIに作業を任せたはずなのに、
人間がAIへ仕事を配り続けている。

→ タスクの割り当てと状態遷移も自動化する。

---

## 統合制御システム

---

並列実行を円滑にするために

* 進行中の issue や 実行待ちの issue を見える化したい
* 後で処理したい issue などをタスクキューとして登録しておきたい
  * 他の作業の完了後に実行したい
  * トークンリミットの都合で今走らせられない

---

### VK Orchestrator の構成

* 作業を実行する AI のルールやスキルなど各種設定  
  → VK Agents  
* 作業を行う実行UI  
  → VK Terminals  
* 進捗を制御するシステム  
  → VK Orchestrator  
  - タスクキュー  
    作業対象の issue を登録する  
	専用リポジトリ（ 別途 ローカルファイルで管理する機能あり ）

---

### 自動処理の流れ

* 作業させる issue をタスクキューとして手動で登録
* そのタスクキューをオーケストレーターが定期巡回
* 実行対象を取り出して VK Terminals の api に実行情報を投げる
  - issue のタイトル
  - issue のURL
  - Claude に投げるスキル など
* VK Terminals が受け取って Claude へ自動投入  
* VK Terminals で状態確認

---

### 専用リポジトリ（タスクキューリポジトリ）の issue を仕事の受付票にする

* 各プロジェクトに issue が存在しているので横断して見にくい
* issue にラベルをつける事で該当ラベルの issue 一覧を表示する事はできるが...
  * 進行ステータスやタスクキュー内での優先度などはラベルで管理するためラベル数が多い  
    → その沢山のラベルを各リポジトリに全部つけるのは美しくない

自動処理の対象の issue を管理する専用のタスクキューリポジトリを作成し、
そのリポジトリの issue に 自動処理する issue を登録していく。

---

1. 各プロジェクトの issue にタスクキュー登録用のラベルを付与  
   ※ チームで運用の場合はアサイン（担当ユーザー）を自分に
2. オーケストレーターが定期巡回、タスクキューラベルを検出
3. 専用のリポジトリの issue に同名の issue を起票  
  中身は元 issue へのリンク

---

### タスクキューリポジトリの issue のラベル

タスクキューリポジトリの issue には、AIが作業を始めるために必要な情報を持たせる。

* アサインユーザー
* 作業対象 issue の url
* 作業ステータス
* 並列でよいか、順番に実行するか
* 優先度
* 自動マージしてよいか

---

### ラベルを状態管理に使う

タスクキューリポジトリに登録されても即時実行はされない

例：

```
status:ready
    ↓
status:in-progress
    ↓
status:waiting-merge
    ↓
status:done
```

---

### オーケストレーター（全体）が行うこと

* 実行可能なタスクを取得
* 優先度と直列・並列条件を判断
* 空きペイン（ターミナル）を作成
* 対象リポジトリの作業ディレクトリを決定
* issue URLをエージェントへ投入
* PR・CI・レビュー状態を監視
* 状態ラベルを更新
* 必要に応じて処理を再開
* 完了条件を満たしたら次のタスクへ

---

### エージェントとオーケストレーターの責務

#### エージェント

* 仕様を理解する
* 実装・テスト・レビューを行う
* PRを作成する
* 結果を報告する

#### オーケストレーター

* タスクを選ぶ
* 実行場所を割り当てる
* 状態を監視する
* 完了条件を判定する
* 次のタスクを流す

---

### 自動化では「完了」の定義が重要

PRが作られた ＝ 完了、ではない。

* 必要なレビューが完了している
* 指摘への対応が終わっている
* CIが通っている
* Draftではない
* マージ可能な状態
* 対象issueとPRが正しく紐づいている

完了条件を機械が判定できる形にする。

---

### 自動マージにもゲートを置く

* エージェントレビュー完了のマーカー
* 現在の head SHA との一致
* CI通過
* 外部レビューが落ち着いている
* mergeable

修正が push されたら、以前のレビュー完了をそのまま信用しない。

---

### 止まった時も自動で再開

長時間動かすと必ず例外が起きる。

* ペインが消えた
* Claude Codeの起動に失敗
* 指示本文が届かなかった
* PR作成前にセッションが止まった
* 一時的にGitHub APIへ接続できない

「成功する経路」だけでなく、
再試行・失敗状態・人間への引き継ぎまで設計する。

---

## 全体像

---

### 最初から全自動を作ったわけではない

1. AIに部分実装させる
2. 不満をルールへ変える
3. 繰り返す作業をスキルへ変える
4. 役割をエージェントへ分けて工程の多いタスクを自動処理できるようにする
5. 複数タスクを並列で動かす
6. 見づらいので統合UIを作る
7. 人間の監視・投入もオーケストレーターへ移す

困りごとを1段ずつ仕組みに変えた結果。

---

## どこまで人間が判断するか

---

### 自動化しやすいのは「正解が判定できる仕事」

* テストが通ったか
* ルール違反がないか
* 必要なレビューが揃ったか
* PRがマージ可能か

判断基準が曖昧な仕事を、そのまま全自動にはできない。

まず人間の暗黙知を
仕様・ルール・テスト・完了条件へ変換する。

---

### 大事だったのは「入力待ち」の検出

AIが止まっている理由は、処理失敗だけではない。

* 権限の確認待ち
* Yes / No の確認待ち
* 選択肢の入力待ち
* 人間の判断待ち

入力待ちのペインを強調し、バッジと通知音で知らせる。

---

### 「全部のログ」より「今どうなっているか」

並列処理の管理で毎回すべてのログを読むのは無理。

まず必要なのは、

* running
* waiting
* idle
* どのタスクか
* PRは作られたか

という状態の要約。

必要な時だけ対象ペインの詳細を見る。

---

## モバイルからの制御

---

### PCの前に張り付かないといけない問題

処理に時間がかかると、

* 食事中
* 移動中
* 会議中
* 外出中

にも「どこかで入力待ちになっていないか」が気になる。

---

### スマートフォン用のWeb画面を追加

ブラウザから全ペインの状態を確認。

* タスクタイトル
* running / waiting / idle
* 直近の出力
* PRへのリンク
* Claude 使用量

アプリのインストールは不要。

---

### スマートフォンから簡単な応答もできる

* 選択肢 `1` / `2` / `3`
* Yes / No
* Enter / Esc / Ctrl-C
* 任意のテキスト送信
* ペインの追加・終了

「状況確認のためだけにPCへ戻る」を減らす。

---

### モバイル操作のセキュリティ

VK Terminals のモバイルページと HTTP API は認証なし。

* 信頼できる自宅・社内LANで使う
* 外出先からは Tailscale などのプライベートネットワーク経由
* 不特定多数が接続できるネットワークへ公開しない
* `0.0.0.0` で安易に待ち受けない

便利さと同時に、操作経路の保護が必要。

---

## それでも人間の作業が残る



---

## 導入するなら

---

### いきなりオーケストレーターを作らなくていい

1. まずAIに小さな実装を頼む
2. 気に入らなかった点を言語化する
3. ルールとして保存する
4. よく使う手順をスキルにする
5. レビューを別のエージェントに任せる
6. 独立したタスクから並列化する
7. 管理作業が負担になったらUI・自動制御を追加する

---

### 先に整えるべきもの

* 仕様書の置き場所
* AGENTS.md / CLAUDE.md からの参照
* コーディング・デザイン・テストのルール
* issue の書き方
* PR の完了条件
* CI・自動テスト
* 人間が承認する境界

AIの数を増やす前に、
判断基準を揃える。

---

## まとめ

---

### 全自動並列AI駆動開発の土台

* 仕様で「何を作るか」を固定する
* ルールで「どう作るか」を共有する
* スキルで「どう進めるか」を再利用する
* エージェントで責任を分ける
* 統合UIで並列処理を見える化する
* オーケストレーターで投入・監視・完了判定を自動化する

---

### AIの能力より、仕事の渡し方が重要

AIが賢くなるほど、
曖昧な指示のまま速く遠くへ進んでしまう。

だから必要なのは、

* 明確な仕様
* 共有されたルール
* 機械判定できる完了条件
* 止まった時に戻れる仕組み

---

# 人間は、コードを書く量を減らし<br>判断の質を上げる

---

### 関連リポジトリ

* VK Agents: https://github.com/vektor-inc/vk-agents/
* VK Terminals: https://github.com/vektor-inc/vk-terminals/
* VK Orchestrator: https://github.com/vektor-inc/vk-orchestrator/
