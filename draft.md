# 全自動並列AI駆動開発システムのしくみ

AIを使ってコードを書くだけではなく、

複数のAIに役割を分担させる
並列でタスクを進める
レビューや修正まで自動化する
人間は全体の判断に集中する
といった開発フローを、効率よく実現するために行った

エージェントの作成
統合UIの作成
自動化システムの作成
モバイルからの制御
について紹介します。

---


* 動作デモ
* ルールを作る
* スキルを作る
* エージェントを作る
* 統合UIを作る

## 

※ はじめに

本セッションでは、AIコーディング自体に慣れてない人にも感覚がわかるように、
ロードマップ的に説明します。
得に前半の説明では「いやいや、そんなやり方しねーよ」って手法から入りますが、
なぜ現状の手法にたどり着くのかを説明するためにあえてセオリーでないかもしれないやり方から説明します。
あらかじめご了承ください。

---

## 仕様は先に作ろう

本セッションの範囲ではないので詳しくは触れませんが、

実際にAIに実装させる前には、AIとディスカッションなどして、作るものの仕様は固めましょう。

* 何を目的とするものなのか
* 技術的な仕様

など、仕様はプロジェクトフォルダの ./docs/ ディレクトリなどに纏めて用意した上で実装してもらいましょう。

---

* どう仕様を作ればいいのかわからない
* 仕様の精度を上げたい

できあいのスキルを使いましょう。

例) superpowers

https://github.com/obra/superpowers

※ 意味がよくわからない人はとりあえず日本語に翻訳してインストールしてみて

---

### 仕様書は AGENTS.md から明示する

./docs/ などのディレクトリに仕様書を置いても明示しないとAIは参照しないので、読み込むように指定します。

```
./docs/ 以下に仕様を纏めてあるので、各作業でAIが参照するようにプロジェクト直下の AGENTAS.md を設定してください。
```

みたいに投げれば良きにはからってくれます。

---

## ルールの整備

---

### 事前に思いつくコーディング規約などを整備

実装にあたって持たせておきたいルールやスキルを設定

* 一般的にスタンダードな規約は読み込ませる
  - WordPressなら WordPress Coding Standards。
  - Pythonなら PEP 8 (命名、整形、docstring)。
  - Goなら gofmt と Go のレビュー規約。

よくわからなければ

「◯◯◯ の開発をするにあたって、
  コーディング規約を作っておきたいです。
　どんなルールにしておくのが一般的？」

みたいに雑に聞いてもそれなりに案内してくれます。

---

### その他事前に用意するルール例

* 関数やメソッドの命名規則
* CSSの命名規則
* デザインシステム
* チーム内の共通独自ルールなど

---

### ルールの保存先

#### 全プロジェクト共通

* Users/ユーザー名/.claude/rules/
* Users/ユーザー名/.codex/rules/

など、ユーザー直下に「. + エージェント名」のディレクトリに .md ファイルで保存されます。

---

### ルールの種類ごとにわける

* Users/ユーザー名/.claude/rules/coding-rules.md
* Users/ユーザー名/.claude/rules/design-rules.md
* Users/ユーザー名/.claude/rules/css-rules.md
* Users/ユーザー名/.claude/rules/e2e-test-rules.md

必要な時だけルールを読み込まないと
* 無駄にトークンを消費する
* 不要なルールまで読み込むと精度が落ちる

---
#### PHPUnit テストの形式
* 自動でテストコードを量産されてもそのテストコードが何をしているのかもわかりにくい
  - 設定値と期待値をセットで記載して、そこだけ確認すれば抑えないといけない動作の漏れがないか確認できるように


---

#### プロジェクト固有のルール

プロジェクトディレクトリ直下の「. + エージェント名」のディレクトリに保存されます。

このあたりは深く考えなくても最初は AI がよしなにしてくれます。

---

## 初期実装

---

### 仕様やルールができたら部分的にテスト実装

最初は Claud Code や Codex みたいなコマンドで実装するのではなく、
VS Code や Cursor などで部分的に実装してもらいます。

AIが実装した内容が確認・チューニングするため

---

### 実際に実装してもらうと...

「いや...そうじゃないんだなぁ...。」

という不満が沢山出ると思います。

---

### ルール化してあったのに...

ルールはある程度読ませてあっても、実際にプロが今まで気を配っていた細かい知識は膨大

* 関数やCSSの命名（一般的ではなく独自ルール）
* テストの書き方
* UIでの説明文のわかりやすさの粒度
* マークアップの作法
* 余白のとり方
* デザインの視線誘導

---

→ A. やっぱりAIは使えない
→ B. 何がダメだったのか、言語化してルールをブラッシュアップしていく

---

### ルールは定期的にブラッシュアップする

運用しているとどんどん追加されて長くなる。

#### 長ければいいわけではない

* AIのトークン消費量が増える
* 長いとそれだけノイズが増えて、ルールが守られなく鳴る。

```
既存のルールファイルについて、表現が誤解を招きやすかったり、説明が冗長になっているか確認して、
AIが誤解無くルールを理解できるようにブラッシュアップしてください。
```

というように精度の見直しをさせます

---

### コーディング以外でもルール化

初期実装させて、以降引き続きプログラムを修正していくと...

ご存知の通りコミットやプルリクエストの作成を繰り返す事になります。

* コミットメッセージを考えるのが面倒
* プルリクの本文を書くのが面倒

→ AI に「コミットして」「プルリク出して」って言えばやってくれるけど...

---

当然今まで社内でやってたルールと違うフォーマットだったりする

* コミットメッセージのルール
* プルリク本文に書く内容や情報の粒度

→ ルール化

---

ルールがあっても人力だと...

* 社内でルールがあっても守らない人がいる
* PRの本文など、雑に書かれると何をチェックすればいいのか意味がわからずレビューに時間がかかる

---

## スキルの作成

---

ルールがある上で、

* 普通に「プルリク出して」とAIに伝えても ルールファイルを読み込んでくれなかったりする
* 「.claude/rules/pull-request-rules/ を読んでプルリク出して」と毎回言うのもだるい

→ スキル化します。

---

### スキルとは？

* よく行う作業の手順書のようなもの
* コマンドで簡単に呼ぼ出せる

---

### スキル例

```
---
name: vk-pr
description: "PR ルールに従い、コミット・changelog 記載・確認手順を含む PR を作成する。PR 作成を依頼されたときに使用"
---

# /vk-pr スキル

このスキルは `rules/pull-request.md` の PR ルールに従い、**PR 作成まで** を担う。

〜 以下略　〜
```

---

実際の pull-request.md で軽く解説

---

./claude/skills/pull-request.md
./claude/skills/edit-title.md

など、ユーザー直下に `.エージェント名/skills/` のディレクトリに .md ファイルで保存されます。

---

### レビュー用のスキル

開発・PR作成をAIがするようになる
→ レビューの数が増える
→ レビュー用のスキルを作る

---

ルールがあっても実装者が100%それに従うとは限らない。

* タイトルがルール通りになってるか
* PR本文がルール通りになってるか？
* 各種コーディングルールが守られているか？
* PHPUnit や e2eテスト がルール通りに書かれているか？
* セキュリティチェック
* UXチェック
* e2eテストを実施＆スクリーンショット撮影
* 結果報告

---

### ルールとスキルの棲み分けが効いてくる

* 実装やPRを立てる時
* レビューする時

ルールは同じ。

実装スキルとレビュースキルで同じ注意事項をそれぞれ書くと...
二重、三重管理になっていく。

* ルールのばらつき
* 片方の変更・追加漏れ

実装スキルもレビュースキルも、同じルールファイルを参照させる。

---

## サブエージェントの作成

---

### 1人のAIに全部やらせると...

* 実装
* テスト
* UI / UX レビュー
* セキュリティレビュー
* PR 作成
* 進行管理

全部同じAIが、自分で実装して、自分でレビューすることになる。

人間のチームに置き換えると、ちょっと不自然。

---

### AIにも役割を持たせる

* ディレクター
* WordPress実装担当
* UI / UX担当
* ブラウザテスト担当
* コード品質・セキュリティ担当

役割ごとに、参照するルール・使うスキル・完了条件を変える。

---

### エージェントは「名前をつけたAI」ではない

必要なのはキャラクター設定よりも、責任範囲の明文化。

* 何を担当するのか
* 何を判断してよいのか
* どのルールを読むのか
* どこまで作業したら完了なのか
* 誰に、何を報告するのか

---

### 役割を分けるメリット

#### コンテキストを分離できる

実装担当は実装に必要な情報へ集中。
レビュー担当は完成した差分を第三者視点で確認。

#### 並列で進められる

依存しない調査・テスト・レビューは同時に実行できる。

#### チェックの独立性が上がる

「自分が書いたコードだから大丈夫」という前提を持たずに確認できる。

---

### 作成したエージェント

| 担当 | 主な役割 |
|---|---|
| 司 | 全体の進行管理・担当者への割り振り |
| 和田 | 主に WordPress 関連などプログラムの実装 |
| 植草 | UI設計・UX・アクセシビリティ |
| 麗美 | Playwright によるブラウザ動作テスト |
| 安藤 | コード品質・セキュリティの最終レビュー |

https://github.com/vektor-inc/vk-agents/

---

### ディレクターに窓口を一本化

人間が各担当へ個別に細かく指示するのではなく、
ディレクター役へ目的と完了条件を伝える。

ディレクターが

1. タスクを整理
2. 担当へ割り振り
3. 結果を回収
4. 不足があれば差し戻し
5. 最終結果を人間へ報告

---

### 例：ひとつの issue を処理する流れ

```
issue
  ↓
司：仕様確認・タスク分解
  ├─ 和田：実装・テスト追加
  ├─ 植草：UI / UX確認
  ├─ 麗美：ブラウザ動作テスト
  └─ 安藤：品質・セキュリティ確認
  ↓
司：指摘を統合・修正を差し戻し
  ↓
PR作成・結果報告
```

---

### ここまでで「一連の自動化」はできた

issue の URL を渡せば、

* 仕様確認
* 実装
* テスト
* 各種レビュー
* PR作成

まで進められるようになった。

---

## 並列実行

---

### タスクが1件ずつなら、まだ普通のAIコーディング

AIは待ち時間が長い。

* 実装中
* テスト実行中
* 外部レビュー待ち
* CI待ち
* 修正後の再確認中

1件が終わるまで待ってから次へ進むのはもったいない。

---

### 複数のタスクを同時に動かす

```
タスク A ─ 実装 ─ テスト ─ レビュー ─ PR
タスク B ─ 実装 ─ テスト ─ レビュー ─ PR
タスク C ─ 実装 ─ テスト ─ レビュー ─ PR
```

AIの処理を速くするのではなく、
待ち時間に別の仕事を流して全体の処理量を増やす。

---

### 並列化で先に決めること

* 同じファイルを同時に触らないか
* タスク同士に依存関係がないか
* どの作業ディレクトリで動かすか
* ブランチや worktree をどう分離するか
* テスト環境のポートが衝突しないか
* どの結果が、どの issue / PR のものか

---

### 並列にすれば全部速くなるわけではない

#### 並列向き

* 別リポジトリの作業
* 変更箇所が重ならないタスク
* 調査やレビュー
* 独立したテスト

#### 直列向き

* 先の仕様・実装結果に依存するタスク
* 同じファイルを大きく変更するタスク
* データ移行など、順序が重要な作業

---

## 統合UIの作成

---

### 標準ターミナルをたくさん開いてみた

並列処理自体はできる。

でも数が増えると...

* どのターミナルが、どのタスクかわからない
* どれが処理中で、どれが入力待ちかわからない
* ウィンドウを順番に開いて確認する必要がある
* 終了したセッションを見落とす
* ターミナルを並べ直すだけでも面倒

---

### AIの処理速度より、人間の監視がボトルネックに

並列数を増やすほど、
人間がターミナルを見回る仕事が増えていく。

→ 複数セッションの状態を一覧できるUIが必要。

---

### VK Terminals

複数のターミナルを自動折返しグリッドで表示する
Electron 製デスクトップアプリを作成。

https://github.com/vektor-inc/vk-terminals/

---

### 複数のClaudeを一画面で操作

* ペインごとに Claude Code を起動
* タスク名を表示
* ペインを追加・並べ替え・リサイズ
* 不要なペインはサイドバーへ格納
* issue や PR へのリンクを表示
* Claude の使用量を表示

※ ここで実際の画面を見せる

---

### 大事だったのは「入力待ち」の検出

AIが止まっている理由は、処理失敗だけではない。

* 権限の確認待ち
* Yes / No の確認待ち
* 選択肢の入力待ち
* 人間の判断待ち

入力待ちのペインを強調し、バッジと通知音で知らせる。

---

### 「全部のログ」より「今どうなっているか」

並列処理の管理で毎回すべてのログを読むのは無理。

まず必要なのは、

* running
* waiting
* idle
* どのタスクか
* PRは作られたか

という状態の要約。

必要な時だけ対象ペインの詳細を見る。

---

## モバイルからの制御

---

### PCの前に張り付かないといけない問題

処理に時間がかかると、

* 食事中
* 移動中
* 会議中
* 外出中

にも「どこかで入力待ちになっていないか」が気になる。

---

### スマートフォン用のWeb画面を追加

ブラウザから全ペインの状態を確認。

* タスクタイトル
* running / waiting / idle
* 直近の出力
* PRへのリンク
* Claude 使用量

アプリのインストールは不要。

---

### スマートフォンから簡単な応答もできる

* 選択肢 `1` / `2` / `3`
* Yes / No
* Enter / Esc / Ctrl-C
* 任意のテキスト送信
* ペインの追加・終了

「状況確認のためだけにPCへ戻る」を減らす。

---

### モバイル操作のセキュリティ

VK Terminals のモバイルページと HTTP API は認証なし。

* 信頼できる自宅・社内LANで使う
* 外出先からは Tailscale などのプライベートネットワーク経由
* 不特定多数が接続できるネットワークへ公開しない
* `0.0.0.0` で安易に待ち受けない

便利さと同時に、操作経路の保護が必要。

---

## それでも人間の作業が残る

---

### 統合UIで「見やすく」はなった

しかし、まだ人間がやっている。

* issue を確認する
* 空いているペインを探す
* タスクを投入する
* 止まっていないか巡回する
* PR・CI・レビューの状態を確認する
* 完了・失敗を判断する
* 次のタスクを投入する

---

### 人間がディスパッチャーになっている

AIに作業を任せたはずなのに、
人間がAIへ仕事を配り続けている。

→ タスクの割り当てと状態遷移も自動化する。

---

## 統合制御システム

---

### VK Orchestrator

タスクキューから実行対象を取り出し、
VK Terminals 上の Claude へ自動投入する制御システム。

https://github.com/vektor-inc/vk-orchestrator/

---

### 3つの役割を分離

```
タスクキュー
  何を実行するか
       ↓
VK Orchestrator
  いつ・どのペインに投入するか
  状態をどう遷移させるか
       ↓
VK Terminals
  Claude Code を実際に動かす
```

---

### issue を仕事の受付票にする

issue には、AIが作業を始めるために必要な情報を持たせる。

* 目的
* 背景
* 完了条件
* 対象リポジトリ
* 優先度
* 並列でよいか、順番に実行するか
* 自動マージしてよいか

---

### ラベルを状態管理に使う

例：

```
status:ready
    ↓
status:in-progress
    ↓
status:waiting-merge
    ↓
status:done
```

失敗したタスクは `status:failed` として、
正常系と分けて確認できるようにする。

---

### オーケストレーターが行うこと

* 実行可能なタスクを取得
* 優先度と直列・並列条件を判断
* 空きペインを作成
* 対象リポジトリの作業ディレクトリを決定
* issue URLをエージェントへ投入
* PR・CI・レビュー状態を監視
* 状態ラベルを更新
* 必要に応じて処理を再開
* 完了条件を満たしたら次のタスクへ

---

### エージェントとオーケストレーターの責務を分ける

#### エージェント

* 仕様を理解する
* 実装・テスト・レビューを行う
* PRを作成する
* 結果を報告する

#### オーケストレーター

* タスクを選ぶ
* 実行場所を割り当てる
* 状態を監視する
* 完了条件を判定する
* 次のタスクを流す

---

### 自動化では「完了」の定義が重要

PRが作られた ＝ 完了、ではない。

* CIが通っている
* 必要なレビューが完了している
* 指摘への対応が終わっている
* Draftではない
* マージ可能な状態
* 対象issueとPRが正しく紐づいている

完了条件を機械が判定できる形にする。

---

### 自動マージにもゲートを置く

* エージェントレビュー完了のマーカー
* 現在の head SHA との一致
* CI通過
* 外部レビューが落ち着いている
* mergeable

修正が push されたら、以前のレビュー完了をそのまま信用しない。

---

### 止まった時も自動で再開

長時間動かすと必ず例外が起きる。

* ペインが消えた
* Claude Codeの起動に失敗
* 指示本文が届かなかった
* PR作成前にセッションが止まった
* 一時的にGitHub APIへ接続できない

「成功する経路」だけでなく、
再試行・失敗状態・人間への引き継ぎまで設計する。

---

## 全体像

---

### 最終的な構成

```
仕様・ルール
     ↓
スキル
     ↓
役割別エージェント
     ↓
VK Terminals（複数セッションの実行・可視化）
     ↓
VK Orchestrator（投入・監視・状態遷移）
     ↓
GitHub issue / PR / CI / レビュー
```

---

### 最初から全自動を作ったわけではない

1. AIに部分実装させる
2. 不満をルールへ変える
3. 繰り返す作業をスキルへ変える
4. 役割をエージェントへ分ける
5. 複数タスクを並列で動かす
6. 見づらいので統合UIを作る
7. 人間の監視・投入もオーケストレーターへ移す

困りごとを1段ずつ仕組みに変えた結果。

---

## どこまで人間が判断するか

---

### 人間を完全に外すことが目的ではない

AIに任せやすいもの：

* 決めたルールに沿った実装
* 定型テスト
* 形式チェック
* 状態監視
* 繰り返しの修正

人間が持つべきもの：

* 何を作るか
* なぜ作るか
* 優先順位
* 仕様上のトレードオフ
* 重大な変更の承認

---

### 自動化しやすいのは「正解が判定できる仕事」

* テストが通ったか
* ルール違反がないか
* 必要なレビューが揃ったか
* PRがマージ可能か

判断基準が曖昧な仕事を、そのまま全自動にはできない。

まず人間の暗黙知を
仕様・ルール・テスト・完了条件へ変換する。

---

## 導入するなら

---

### いきなりオーケストレーターを作らなくていい

1. まずAIに小さな実装を頼む
2. 気に入らなかった点を言語化する
3. ルールとして保存する
4. よく使う手順をスキルにする
5. レビューを別のエージェントに任せる
6. 独立したタスクから並列化する
7. 管理作業が負担になったらUI・自動制御を追加する

---

### 先に整えるべきもの

* 仕様書の置き場所
* AGENTS.md / CLAUDE.md からの参照
* コーディング・デザイン・テストのルール
* issue の書き方
* PR の完了条件
* CI・自動テスト
* 人間が承認する境界

AIの数を増やす前に、
判断基準を揃える。

---

## まとめ

---

### 全自動並列AI駆動開発の土台

* 仕様で「何を作るか」を固定する
* ルールで「どう作るか」を共有する
* スキルで「どう進めるか」を再利用する
* エージェントで責任を分ける
* 統合UIで並列処理を見える化する
* オーケストレーターで投入・監視・完了判定を自動化する

---

### AIの能力より、仕事の渡し方が重要

AIが賢くなるほど、
曖昧な指示のまま速く遠くへ進んでしまう。

だから必要なのは、

* 明確な仕様
* 共有されたルール
* 機械判定できる完了条件
* 止まった時に戻れる仕組み

---

# 人間は、コードを書く量を減らし<br>判断の質を上げる

---

### 関連リポジトリ

* VK Agents: https://github.com/vektor-inc/vk-agents/
* VK Terminals: https://github.com/vektor-inc/vk-terminals/
* VK Orchestrator: https://github.com/vektor-inc/vk-orchestrator/
